{{#each books as |book|}}

  <!-- {{> partial}}
  Don't uncomment yet -->

   <!-- Developers, why are we using data-id and not just id? -->
   <ul data-id="{{books.id}}">
     <li> <small>Title: {{books.title}}</small></li>
     {{#if books.author}}
        <li>Author: {{books.author}}</li>
     {{else}}
        <li>No description.</li>
      {{/if}}
   </ul>

{{/each}}
